/* Final QA3 visual alignment for mechanics refined after the main visual pass. */
(()=>{const prior=renderDomainVisual;renderDomainVisual=function(){if(STATE.mission.id!=='06.02')return prior();const mode=vs('mode','POSITION_PID'),g=vv('gain',350),ki=vv('integral',20),d=vv('damp',45),err=vv('error',.03),contact=vv('contact',0),track=mode==='POSITION_PID'?err/(1+g/180+ki/120):err,overshoot=mode==='POSITION_PID'?g/(d+20):0,windup=mode==='POSITION_PID'?ki/(d+10):0,force=contact*(mode==='IMPEDANCE'?g*err:(g*err+.18*ki));const box=visualBase('PID ↔ IMPEDANCE OBJECTIVE');box.insertAdjacentHTML('beforeend',`<div class="viz-center">${vchips([[`MODE · ${mode}`,mode==='IMPEDANCE'?'good':'']])}${vbar('P / STIFFNESS',g,50,900,vfmt(g,0))}${vbar('I ACTION',ki,0,80,vfmt(ki,0))}${vbar('D / DAMPING',d,5,120,vfmt(d,0))}${mode==='POSITION_PID'?vbar('TRACK ERROR',track,0,.04,`${vfmt(track*1000,1)} mm`)+vbar('OVERSHOOT PROXY',overshoot,0,8,vfmt(overshoot,2))+vbar('WINDUP PROXY',windup,0,4,vfmt(windup,2)):vbar('CONTACT FORCE',force,0,60,`${vfmt(force,1)} N`)+vbar('DAMPING RATIO',d/Math.sqrt(Math.max(g,1)),0,4,vfmt(d/Math.sqrt(Math.max(g,1)),2))}</div>`);};})();