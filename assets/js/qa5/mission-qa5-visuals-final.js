/* PLAY//LEARN QA5 — final visual alignment for 07.04. */
(()=>{const prior=renderDomainVisual;renderDomainVisual=function(){if(STATE.mission.id!=='07.04')return prior();const m=vv('sinkMass',.7),p=vv('fanPower',30),block=vv('blockage',0),r=.48/(1+1.8*m),active=1+(p/45)*Math.max(.05,1-block),temp=30+vv('loss',180)*r/active,cost=m+p/60;const box=visualBase('PASSIVE MASS ↔ ACTIVE POWER');box.insertAdjacentHTML('beforeend',`<div class="viz-center">${vpair(`HEATSINK ${vfmt(m,1)} kg`,'↔',`COOLING ${vfmt(p,0)} W`,`same thermal budget`)}${vbar('PASSIVE Rθ',r,.05,.4,`${vfmt(r,3)} K/W`)}${vbar('ACTIVE COOLING',active,1,3,`${vfmt(active,2)}×`)}${vbar('SURFACE TEMP',temp,30,90,`${vfmt(temp,1)}°C`)}${vbar('SYSTEM COST',cost,0,2,`${vfmt(cost,2)} / ${vfmt(vv('thermalBudget',1.5),1)}`)}</div>`);};})();